#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    int n, a, b, c, i, ans = 0;
    cin >> n >> a >> b >> c;
    vector<int> l(n);
    for (i = 0; i < n; i++) cin >> l.at(i);
    vector< vector <int> > dp(3, vector<int>(n, 0));

    for(i = 0; i < n; i++) {
        
    }

    return 0;
}